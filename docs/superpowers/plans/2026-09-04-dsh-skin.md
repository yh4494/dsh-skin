# dsh-skin Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 Electron 桌面壳启动本机 `dsh web`，以 WebView 加载本地 URL，并支持托盘显隐与退出时清理子进程。

**Architecture:** Electron 主进程负责解析 `dsh`、探测端口、按需 spawn `dsh web --no-open --port <port>`、轮询 HTTP 就绪后 `BrowserWindow.loadURL`。关闭窗口只 hide 到 Tray；托盘「退出」仅杀掉本壳 spawn 的子进程。纯逻辑（配置、路径、HTTP 探测、进程启停）抽到可单测模块，`main.js` 只做 Electron 编排。

**Tech Stack:** Electron（最新稳定版）、Node 内置 `node:test` + `node:assert`、macOS。

## Global Constraints

- 平台：首版仅 macOS
- 默认端口：`18789`；环境变量 `DSH_SKIN_PORT` 可覆盖
- 启动命令：`dsh web --no-open --port <port>`
- 关窗口 → hide，不杀 dsh；托盘退出才 quit
- 复用已有端口服务时，退出 App **不**杀外部进程
- 不修改 `~/.dsh`、不打包内置 dsh、不做设置页/多 profile/皮肤切换
- 仓库若尚未 `git init`，Task 1 先初始化；用户未要求提交时可跳过各 Task 的 commit 步骤，但文件变更仍须完成

---

## File Map

| File | Responsibility |
|------|----------------|
| `package.json` | scripts、electron 依赖、`"main": "src/main.js"` |
| `src/config.js` | 端口与 base URL |
| `src/resolve-dsh.js` | 解析本机 `dsh` 可执行路径 |
| `src/http-ready.js` | 端口是否已有 HTTP、轮询就绪 |
| `src/dsh-launcher.js` | spawn / stop / owned 标记 |
| `src/main.js` | Electron：窗口、托盘、编排启动 |
| `src/preload.js` | 向 loading 页暴露 `retry` IPC |
| `src/loading.html` | 启动中 / 错误 + 重试 |
| `assets/trayTemplate.png` | macOS 菜单栏模板图标（黑白） |
| `test/*.test.js` | 纯模块单测 |
| `README.md` | 安装与运行说明 |

---

### Task 1: 脚手架 + config

**Files:**
- Create: `package.json`
- Create: `src/config.js`
- Create: `test/config.test.js`
- Create: `.gitignore`

**Interfaces:**
- Consumes: 无
- Produces:
  - `getPort(): number` — 读 `process.env.DSH_SKIN_PORT`，非法或空则 `18789`
  - `getBaseUrl(port?: number): string` — `http://127.0.0.1:${port ?? getPort()}`

- [ ] **Step 1: 写失败单测**

```js
// test/config.test.js
const { describe, it, beforeEach, afterEach } = require('node:test');
const assert = require('node:assert/strict');
const { getPort, getBaseUrl } = require('../src/config.js');

describe('config', () => {
  const prev = process.env.DSH_SKIN_PORT;
  afterEach(() => {
    if (prev === undefined) delete process.env.DSH_SKIN_PORT;
    else process.env.DSH_SKIN_PORT = prev;
  });

  it('defaults to 18789', () => {
    delete process.env.DSH_SKIN_PORT;
    assert.equal(getPort(), 18789);
  });

  it('reads DSH_SKIN_PORT', () => {
    process.env.DSH_SKIN_PORT = '19000';
    assert.equal(getPort(), 19000);
  });

  it('falls back on invalid port', () => {
    process.env.DSH_SKIN_PORT = 'nope';
    assert.equal(getPort(), 18789);
  });

  it('builds base url', () => {
    assert.equal(getBaseUrl(18789), 'http://127.0.0.1:18789');
  });
});
```

- [ ] **Step 2: 跑测确认失败**

Run: `node --test test/config.test.js`  
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 config + package.json + gitignore**

```js
// src/config.js
'use strict';

const DEFAULT_PORT = 18789;

function getPort() {
  const raw = process.env.DSH_SKIN_PORT;
  if (raw == null || raw === '') return DEFAULT_PORT;
  const n = Number(raw);
  if (!Number.isInteger(n) || n < 1 || n > 65535) return DEFAULT_PORT;
  return n;
}

function getBaseUrl(port = getPort()) {
  return `http://127.0.0.1:${port}`;
}

module.exports = { getPort, getBaseUrl, DEFAULT_PORT };
```

```json
{
  "name": "dsh-skin",
  "version": "0.1.0",
  "private": true,
  "description": "Local Electron shell for DeepSeek Harness (dsh web)",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "electron": "^37.0.0"
  }
}
```

```gitignore
node_modules/
dist/
.DS_Store
*.log
```

若尚无 git：`git init`。

- [ ] **Step 4: 安装依赖并跑通单测**

Run: `npm install && npm test`  
Expected: `config` 相关测试 PASS

- [ ] **Step 5: Commit（若用户要求提交）**

```bash
git add package.json package-lock.json .gitignore src/config.js test/config.test.js
git commit -m "$(cat <<'EOF'
chore: scaffold dsh-skin with port config

EOF
)"
```

---

### Task 2: 解析 dsh 路径

**Files:**
- Create: `src/resolve-dsh.js`
- Create: `test/resolve-dsh.test.js`

**Interfaces:**
- Consumes: 无
- Produces:
  - `resolveDshPath({ whichSync?, existsSync?, homedir?, env? } = {}): string | null`
  - 先查 `env.DSH_BIN`（若存在且文件存在），再 `whichSync('dsh')`，再扫描 `homedir()/.nvm/versions/node/*/bin/dsh`（取 mtime 最新或路径排序最后一个存在的）

说明：注入 `whichSync`/`existsSync` 便于单测，默认实现用 `child_process.execFileSync('which', ['dsh'])` 与 `fs.existsSync`。

- [ ] **Step 1: 写失败单测**

```js
// test/resolve-dsh.test.js
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const { resolveDshPath } = require('../src/resolve-dsh.js');

describe('resolveDshPath', () => {
  it('prefers DSH_BIN when file exists', () => {
    const path = resolveDshPath({
      env: { DSH_BIN: '/custom/dsh' },
      existsSync: (p) => p === '/custom/dsh',
      whichSync: () => '/usr/bin/dsh',
      homedir: () => '/Users/x',
    });
    assert.equal(path, '/custom/dsh');
  });

  it('uses which when DSH_BIN missing', () => {
    const path = resolveDshPath({
      env: {},
      existsSync: () => false,
      whichSync: () => '/opt/homebrew/bin/dsh',
      homedir: () => '/Users/x',
    });
    assert.equal(path, '/opt/homebrew/bin/dsh');
  });

  it('returns null when nothing found', () => {
    const path = resolveDshPath({
      env: {},
      existsSync: () => false,
      whichSync: () => null,
      homedir: () => '/Users/x',
      nvmCandidates: () => [],
    });
    assert.equal(path, null);
  });
});
```

- [ ] **Step 2: 跑测确认失败**

Run: `node --test test/resolve-dsh.test.js`  
Expected: FAIL

- [ ] **Step 3: 实现 resolve-dsh.js**

```js
// src/resolve-dsh.js
'use strict';

const fs = require('node:fs');
const os = require('node:os');
const path = require('node:path');
const { execFileSync } = require('node:child_process');

function defaultWhichSync(cmd) {
  try {
    return execFileSync('which', [cmd], { encoding: 'utf8' }).trim() || null;
  } catch {
    return null;
  }
}

function defaultNvmCandidates(homedir) {
  const base = path.join(homedir, '.nvm', 'versions', 'node');
  if (!fs.existsSync(base)) return [];
  return fs
    .readdirSync(base)
    .map((v) => path.join(base, v, 'bin', 'dsh'))
    .filter((p) => fs.existsSync(p));
}

function resolveDshPath(deps = {}) {
  const env = deps.env ?? process.env;
  const existsSync = deps.existsSync ?? fs.existsSync.bind(fs);
  const whichSync = deps.whichSync ?? defaultWhichSync;
  const homedir = deps.homedir ?? (() => os.homedir());
  const nvmCandidates = deps.nvmCandidates ?? (() => defaultNvmCandidates(homedir()));

  if (env.DSH_BIN && existsSync(env.DSH_BIN)) return env.DSH_BIN;

  const fromWhich = whichSync('dsh');
  if (fromWhich && existsSync(fromWhich)) return fromWhich;

  const nvm = nvmCandidates();
  if (nvm.length > 0) return nvm[nvm.length - 1];

  return null;
}

module.exports = { resolveDshPath };
```

- [ ] **Step 4: 跑测确认通过**

Run: `npm test`  
Expected: PASS

- [ ] **Step 5: Commit（若用户要求）**

```bash
git add src/resolve-dsh.js test/resolve-dsh.test.js
git commit -m "$(cat <<'EOF'
feat: resolve local dsh binary path

EOF
)"
```

---

### Task 3: HTTP 就绪探测

**Files:**
- Create: `src/http-ready.js`
- Create: `test/http-ready.test.js`

**Interfaces:**
- Consumes: 无（使用传入的 `fetch`）
- Produces:
  - `async isHttpReady(baseUrl, { fetch?, timeoutMs? } = {}): Promise<boolean>` — GET `baseUrl`，任意网络完成且非 `ECONNREFUSED` 类失败即 true；超时/连接拒绝为 false
  - `async waitForHttp(baseUrl, { fetch?, intervalMs?, timeoutMs? } = {}): Promise<void>` — 轮询直到 ready，否则 throw `Error('dsh did not become ready in time')`

- [ ] **Step 1: 写失败单测**

```js
// test/http-ready.test.js
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const { isHttpReady, waitForHttp } = require('../src/http-ready.js');

describe('http-ready', () => {
  it('isHttpReady true on 200', async () => {
    const fetch = async () => ({ ok: true, status: 200 });
    assert.equal(await isHttpReady('http://127.0.0.1:9', { fetch }), true);
  });

  it('isHttpReady false on connection error', async () => {
    const fetch = async () => {
      throw Object.assign(new Error('fail'), { code: 'ECONNREFUSED' });
    };
    assert.equal(await isHttpReady('http://127.0.0.1:9', { fetch }), false);
  });

  it('waitForHttp resolves when ready', async () => {
    let n = 0;
    const fetch = async () => {
      n += 1;
      if (n < 2) throw Object.assign(new Error('no'), { code: 'ECONNREFUSED' });
      return { ok: true, status: 200 };
    };
    await waitForHttp('http://127.0.0.1:9', { fetch, intervalMs: 10, timeoutMs: 500 });
  });

  it('waitForHttp throws on timeout', async () => {
    const fetch = async () => {
      throw Object.assign(new Error('no'), { code: 'ECONNREFUSED' });
    };
    await assert.rejects(
      () => waitForHttp('http://127.0.0.1:9', { fetch, intervalMs: 10, timeoutMs: 50 }),
      /ready/,
    );
  });
});
```

- [ ] **Step 2: 跑测确认失败**

Run: `node --test test/http-ready.test.js`  
Expected: FAIL

- [ ] **Step 3: 实现 http-ready.js**

```js
// src/http-ready.js
'use strict';

async function isHttpReady(baseUrl, opts = {}) {
  const fetchFn = opts.fetch ?? globalThis.fetch;
  const timeoutMs = opts.timeoutMs ?? 1500;
  const ctrl = new AbortController();
  const t = setTimeout(() => ctrl.abort(), timeoutMs);
  try {
    await fetchFn(baseUrl, { signal: ctrl.signal, redirect: 'manual' });
    return true;
  } catch {
    return false;
  } finally {
    clearTimeout(t);
  }
}

async function waitForHttp(baseUrl, opts = {}) {
  const intervalMs = opts.intervalMs ?? 250;
  const timeoutMs = opts.timeoutMs ?? 60000;
  const start = Date.now();
  while (Date.now() - start < timeoutMs) {
    if (await isHttpReady(baseUrl, opts)) return;
    await new Promise((r) => setTimeout(r, intervalMs));
  }
  throw new Error('dsh did not become ready in time');
}

module.exports = { isHttpReady, waitForHttp };
```

- [ ] **Step 4: 跑测确认通过**

Run: `npm test`  
Expected: PASS

- [ ] **Step 5: Commit（若用户要求）**

```bash
git add src/http-ready.js test/http-ready.test.js
git commit -m "$(cat <<'EOF'
feat: poll local HTTP until dsh web is ready

EOF
)"
```

---

### Task 4: dsh 进程启停

**Files:**
- Create: `src/dsh-launcher.js`
- Create: `test/dsh-launcher.test.js`

**Interfaces:**
- Consumes: `resolveDshPath`（可注入）、`isHttpReady` / `waitForHttp`（可注入）
- Produces: class / factory `createLauncher(deps)` 返回：
  - `owned: boolean` — 是否由本 launcher spawn
  - `child: ChildProcess | null`
  - `async start({ port, baseUrl }): Promise<{ reused: boolean }>`  
    - 若 `isHttpReady(baseUrl)` → `{ reused: true }`，不 spawn，`owned=false`  
    - 否则 `resolveDshPath()`，null 则 throw `Error('dsh not found')`  
    - spawn `[bin, 'web', '--no-open', '--port', String(port)]`，`owned=true`，再 `waitForHttp`；失败则 `stop()` 后 rethrow
  - `async stop(): Promise<void>` — 仅当 `owned && child`：SIGTERM，1.5s 后未退出则 SIGKILL；然后清空 child、owned=false
  - `onExit(cb)` — 子进程意外 exit 时回调（非 `stop` 触发的可区分：用内部 `stopping` 标志）

- [ ] **Step 1: 写失败单测（用假 spawn）**

```js
// test/dsh-launcher.test.js
const { describe, it } = require('node:test');
const assert = require('node:assert/strict');
const { EventEmitter } = require('node:events');
const { createLauncher } = require('../src/dsh-launcher.js');

function fakeChild() {
  const ee = new EventEmitter();
  ee.killed = false;
  ee.exitCode = null;
  ee.kill = (sig) => {
    ee.killed = true;
    ee.exitCode = 0;
    queueMicrotask(() => ee.emit('exit', 0, sig));
    return true;
  };
  return ee;
}

describe('dsh-launcher', () => {
  it('reuses existing server without spawn', async () => {
    let spawned = false;
    const launcher = createLauncher({
      isHttpReady: async () => true,
      waitForHttp: async () => {},
      resolveDshPath: () => '/bin/dsh',
      spawn: () => {
        spawned = true;
        return fakeChild();
      },
    });
    const r = await launcher.start({ port: 18789, baseUrl: 'http://127.0.0.1:18789' });
    assert.equal(r.reused, true);
    assert.equal(spawned, false);
    assert.equal(launcher.owned, false);
  });

  it('spawns when not ready', async () => {
    const child = fakeChild();
    const launcher = createLauncher({
      isHttpReady: async () => false,
      waitForHttp: async () => {},
      resolveDshPath: () => '/bin/dsh',
      spawn: (cmd, args) => {
        assert.equal(cmd, '/bin/dsh');
        assert.deepEqual(args, ['web', '--no-open', '--port', '18789']);
        return child;
      },
    });
    const r = await launcher.start({ port: 18789, baseUrl: 'http://127.0.0.1:18789' });
    assert.equal(r.reused, false);
    assert.equal(launcher.owned, true);
  });

  it('stop kills owned child', async () => {
    const child = fakeChild();
    const launcher = createLauncher({
      isHttpReady: async () => false,
      waitForHttp: async () => {},
      resolveDshPath: () => '/bin/dsh',
      spawn: () => child,
    });
    await launcher.start({ port: 1, baseUrl: 'http://127.0.0.1:1' });
    await launcher.stop();
    assert.equal(child.killed, true);
    assert.equal(launcher.owned, false);
  });
});
```

- [ ] **Step 2: 跑测确认失败**

Run: `node --test test/dsh-launcher.test.js`  
Expected: FAIL

- [ ] **Step 3: 实现 dsh-launcher.js**

按 Interfaces 实现；`spawn` 默认 `require('child_process').spawn`，stdio 可 `ignore` 或 `pipe` 到主进程 log（首版 `inherit` 便于调试）。`stop` 必须设置 `stopping=true` 再 kill，避免误触发「意外退出」回调。

- [ ] **Step 4: 跑测确认通过**

Run: `npm test`  
Expected: PASS

- [ ] **Step 5: Commit（若用户要求）**

```bash
git add src/dsh-launcher.js test/dsh-launcher.test.js
git commit -m "$(cat <<'EOF'
feat: start and stop owned dsh web process

EOF
)"
```

---

### Task 5: loading 页 + preload + 最小窗口

**Files:**
- Create: `src/preload.js`
- Create: `src/loading.html`
- Create: `src/main.js`（先只开窗口加载 loading，不启 dsh）
- Create: `assets/trayTemplate.png`（16×16 或 32×32 黑白 PNG；可用一行脚本生成）

**Interfaces:**
- Consumes: 无
- Produces:
  - preload：`contextBridge.exposeInMainWorld('dshSkin', { retry: () => ipcRenderer.send('dsh-skin:retry') })`
  - loading.html：默认文案「正在启动 dsh…」；可通过 query `?error=` 显示错误与「重试」按钮（调用 `window.dshSkin.retry()`）
  - main：创建隐藏菜单栏或保留默认）、`BrowserWindow` 1200×800，`loadFile('src/loading.html')`，`webPreferences: { preload, contextIsolation: true, nodeIntegration: false }`

- [ ] **Step 1: 实现 preload + loading.html**

`loading.html` 简洁可读：居中标题、错误段落、重试按钮（无 error 时隐藏按钮）。

- [ ] **Step 2: 实现最小 main.js**

```js
// 骨架要点
const { app, BrowserWindow } = require('electron');
const path = require('node:path');

let mainWindow;

function createWindow() {
  mainWindow = new BrowserWindow({
    width: 1200,
    height: 800,
    webPreferences: {
      preload: path.join(__dirname, 'preload.js'),
      contextIsolation: true,
      nodeIntegration: false,
    },
  });
  mainWindow.loadFile(path.join(__dirname, 'loading.html'));
}

app.whenReady().then(createWindow);
app.on('window-all-closed', (e) => {
  // 后续 Task 改为 hide；本 Task 可先 prevent 默认退出以便托盘任务接入
});
```

- [ ] **Step 3: 生成 tray 占位图**

Run（示例）：

```bash
# 用 sips/python 生成简单 32x32 PNG 到 assets/trayTemplate.png
python3 - <<'PY'
from pathlib import Path
# minimal valid 1x1 PNG expanded — or use PIL if available
import struct, zlib
def png(w,h,rgb=(0,0,0)):
    def chunk(t,d):
        return struct.pack('>I',len(d))+t+d+struct.pack('>I',zlib.crc32(t+d)&0xffffffff)
    raw=b''.join(b'\\x00'+bytes(rgb)*w for _ in range(h))
    return b'\\x89PNG\\r\\n\\x1a\\n'+chunk(b'IHDR',struct.pack('>IIBBBBB',w,h,8,2,0,0,0))+chunk(b'IDAT',zlib.compress(raw))+chunk(b'IEND',b'')
Path('assets').mkdir(exist_ok=True)
Path('assets/trayTemplate.png').write_bytes(png(32,32))
PY
```

- [ ] **Step 4: 手工验证窗口**

Run: `npm start`  
Expected: 出现 Electron 窗口，显示「正在启动 dsh…」（本 Task 可停在 loading）

- [ ] **Step 5: Commit（若用户要求）**

```bash
git add src/main.js src/preload.js src/loading.html assets/trayTemplate.png
git commit -m "$(cat <<'EOF'
feat: add Electron window and loading page

EOF
)"
```

---

### Task 6: 编排启动 — spawn/复用 + loadURL

**Files:**
- Modify: `src/main.js`
- Modify: `src/loading.html`（若需）

**Interfaces:**
- Consumes: `getPort`, `getBaseUrl`, `createLauncher`, `isHttpReady`
- Produces: `async bootDsh()` — start launcher → 成功则 `mainWindow.loadURL(baseUrl)`；失败则 `loadFile(loading.html?error=encodeURIComponent(msg))`
- IPC：`ipcMain.on('dsh-skin:retry', () => bootDsh())`
- launcher `onExit`：非 stopping 时加载错误页「dsh 已退出」

- [ ] **Step 1: 在 main.js 接入 createLauncher 与 bootDsh**

要点：

```js
const { getPort, getBaseUrl } = require('./config');
const { createLauncher } = require('./dsh-launcher');
const launcher = createLauncher();

async function bootDsh() {
  const port = getPort();
  const baseUrl = getBaseUrl(port);
  mainWindow.loadFile(path.join(__dirname, 'loading.html'));
  try {
    await launcher.start({ port, baseUrl });
    await mainWindow.loadURL(baseUrl);
  } catch (err) {
    const q = new URLSearchParams({ error: String(err.message || err) });
    await mainWindow.loadFile(path.join(__dirname, 'loading.html'), { search: q.toString() });
  }
}
```

注意：`loadFile` 的 search 在 Electron 里用 `loadFile(path, { query: { error: '...' } })`。

- [ ] **Step 2: `app.whenReady` 后 createWindow + bootDsh**

- [ ] **Step 3: 手工验证冷启动**

Run: `npm start`（确保本机 `dsh` 可用、18789 空闲）  
Expected: loading → 窗口内出现 dsh Web UI（不是系统浏览器）

- [ ] **Step 4: 手工验证端口复用**

另开终端：`dsh web --no-open --port 18789`，再 `npm start`  
Expected: 不双开冲突；窗口加载同一 UI

- [ ] **Step 5: Commit（若用户要求）**

```bash
git add src/main.js
git commit -m "$(cat <<'EOF'
feat: boot dsh web into BrowserWindow

EOF
)"
```

---

### Task 7: 托盘显隐 + 退出清理

**Files:**
- Modify: `src/main.js`

**Interfaces:**
- Consumes: `launcher.stop`
- Produces:
  - `Tray` + `Menu`：`显示` → show/focus；`退出` → `await launcher.stop(); app.quit()`
  - `mainWindow.on('close', (e) => { if (!isQuitting) { e.preventDefault(); mainWindow.hide(); } })`
  - `app.on('before-quit', () => { isQuitting = true; })` 并确保 stop 已调用（退出菜单路径）
  - `app.on('activate', () => mainWindow.show())`（Dock 点击）
  - `app.dock?.show()` 保持 Dock 图标（可选）
  - 托盘图标：`nativeImage.createFromPath(assets/trayTemplate.png).setTemplateImage(true)`

- [ ] **Step 1: 实现 Tray 与 close→hide**

- [ ] **Step 2: 实现退出路径只杀 owned 进程**

`launcher.stop()` 已保证非 owned 为空操作。

- [ ] **Step 3: 手工验证切换**

关窗口 → App 不退、托盘在；托盘「显示」唤回；浏览器访问 `http://127.0.0.1:18789` 仍可用。

- [ ] **Step 4: 手工验证退出**

托盘「退出」→ 若本次 spawn 的 dsh，端口释放；若复用外部 dsh，外部仍存活。

- [ ] **Step 5: Commit（若用户要求）**

```bash
git add src/main.js
git commit -m "$(cat <<'EOF'
feat: tray show/hide and quit stops owned dsh

EOF
)"
```

---

### Task 8: README + 收尾自检

**Files:**
- Create: `README.md`

**Interfaces:** 无

- [ ] **Step 1: 写 README**

内容须包含：

- 依赖：已安装 Node、本机可用 `dsh`（`npm i -g @deepseek-ai/dsh` 或现有方式）
- `npm install` / `npm start` / `npm test`
- 环境变量：`DSH_SKIN_PORT`、`DSH_BIN`（resolve-dsh 已支持）
- 行为摘要：关窗进托盘；托盘退出才停本壳拉起的 dsh

- [ ] **Step 2: 跑全量测试**

Run: `npm test`  
Expected: 全部 PASS

- [ ] **Step 3: 对照 spec 手工清单勾一遍**

对照 `docs/superpowers/specs/2026-09-04-dsh-skin-design.md` 测试要点 1–5。

- [ ] **Step 4: Commit（若用户要求）**

```bash
git add README.md
git commit -m "$(cat <<'EOF'
docs: add dsh-skin README

EOF
)"
```

---

## Spec Coverage Check

| Spec 要求 | Task |
|-----------|------|
| Electron 窗口 WebView 加载本地 URL | 5, 6 |
| spawn `dsh web --no-open --port` | 4, 6 |
| 默认端口 18789 / `DSH_SKIN_PORT` | 1 |
| 端口已占用则复用、退出不杀外部 | 4, 6, 7 |
| loading / 错误重试 | 5, 6 |
| 关窗 hide + 托盘显示 | 7 |
| Dock activate 唤回 | 7 |
| 托盘退出停 owned 子进程 | 4, 7 |
| 解析 PATH + nvm | 2 |
| 不改 ~/.dsh / 无设置页 | 全局约束 |
| README | 8 |

## Self-Review Notes

- 无 TBD；`DSH_BIN` 在 Task 2 一并实现（与 resolve 测试一致），README 在 Task 8 文档化
- 纯逻辑与 Electron 编排分离，便于 `node:test` 覆盖
- `loadFile` query 使用 Electron `{ query }` API，避免手写 search 踩坑

{
  "name": "dsh-skin",
  "version": "0.1.0",
  "private": true,
  "description": "Local Electron shell for DeepSeek Harness (dsh web)",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "electron": "^37.0.0"
  }
}
